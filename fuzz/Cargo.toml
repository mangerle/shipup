[package]
name = "shipup-fuzz"
version = "0.0.0"
publish = false
edition = "2024"

# 独立于根 workspace，避免 cargo-fuzz 报 "believes it's in a workspace"
[workspace]

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
serde_json = "1.0"
shipup = { path = "../shipup", default-features = true }

[[bin]]
name = "fuzz_manifest"
path = "fuzz_targets/fuzz_manifest.rs"
test = false
doc = false

[[bin]]
name = "fuzz_archive"
path = "fuzz_targets/fuzz_archive.rs"
test = false
doc = false
